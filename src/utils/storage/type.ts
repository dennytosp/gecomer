enum AsyncKey {
  ENV = 'ENV',
  LANGUAGE = 'settings.lang',
}

enum EncryptedKey {
  SECURE_DEVICE_ID = 'SECURE_DEVICE_ID',
}

enum ENVName {
  Development = 'Development',
  Staging = 'Staging',
  Production = 'Production',
}

export { AsyncKey, ENVName, EncryptedKey }
